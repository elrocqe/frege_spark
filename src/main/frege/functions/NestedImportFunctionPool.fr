-- module that contains functions which can be executed on spark nodes, to be imported in other modules

module functions.NestedImportFunctionPool where

  addThree :: Double -> Double
  addThree x = x + 3.0
  